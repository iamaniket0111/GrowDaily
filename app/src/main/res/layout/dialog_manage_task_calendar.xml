<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/dialog_surface_card"
    android:orientation="vertical"
    android:padding="22dp">

    <!-- Month Switcher Header -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:paddingBottom="12dp">

        <TextView
            android:id="@+id/txtMonth"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:textColor="@color/dialog_text_primary"
            android:textSize="20sp"
            android:fontFamily="sans-serif-medium"
            android:textStyle="normal" />

        <ImageButton
            android:id="@+id/btnPrevMonth"
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/prev_month"
            android:src="@drawable/ic_left_arrow"
            app:tint="@color/task_text_secondary" />

        <Space
            android:layout_width="8dp"
            android:layout_height="wrap_content" />

        <ImageButton
            android:id="@+id/btnNextMonth"
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/next_month"
            android:src="@drawable/ic_right_arrow"
            app:tint="@color/task_text_secondary" />
    </LinearLayout>

    <!-- Weekday Header Row -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:orientation="horizontal"
        android:weightSum="7">

        <TextView style="@style/CalendarHeaderDay" android:text="S" />
        <TextView style="@style/CalendarHeaderDay" android:text="M" />
        <TextView style="@style/CalendarHeaderDay" android:text="T" />
        <TextView style="@style/CalendarHeaderDay" android:text="W" />
        <TextView style="@style/CalendarHeaderDay" android:text="T" />
        <TextView style="@style/CalendarHeaderDay" android:text="F" />
        <TextView style="@style/CalendarHeaderDay" android:text="S" />
    </LinearLayout>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerDays"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:overScrollMode="never" />

    <!-- Polished Legend Row -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <View
            android:id="@+id/indicatorOriginal"
            android:layout_width="12dp"
            android:layout_height="12dp" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="6dp"
            android:text="@string/calendar_start_date_label"
            android:textColor="@color/task_text_secondary"
            android:textSize="11sp"
            android:fontFamily="sans-serif-medium" />

        <View
            android:id="@+id/indicatorActive"
            android:layout_width="12dp"
            android:layout_height="12dp"
            android:layout_marginStart="14dp" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="6dp"
            android:text="@string/calendar_active_date_label"
            android:textColor="@color/task_text_secondary"
            android:textSize="11sp"
            android:fontFamily="sans-serif-medium" />
    </LinearLayout>

    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:layout_marginTop="16dp"
        android:background="@color/task_surface_stroke" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="12dp"
        android:lineSpacingExtra="3dp"
        android:textColor="@color/task_text_secondary"
        android:textSize="12sp"
        android:text="@string/calendar_toggle_hint" />

    <Button
        android:id="@+id/btnDone"
        style="@style/DialogPrimaryButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:text="@string/done_button" />

</LinearLayout>
